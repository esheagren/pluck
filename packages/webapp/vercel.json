{
  "buildCommand": "cd ../.. && npm install && npm run build:webapp",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
